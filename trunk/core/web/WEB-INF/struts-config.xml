<?xml version="1.0" encoding="ISO-8859-1" ?>

<!DOCTYPE struts-config PUBLIC
          "-//Apache Software Foundation//DTD Struts Configuration 1.2//EN"
          "http://jakarta.apache.org/struts/dtds/struts-config_1_2.dtd">

<!--
     This is a blank Struts configuration file with an example
     welcome action/page and other commented sample elements.

     Tiles and the Struts Validator are configured using the factory defaults
     and are ready-to-use.

     NOTE: If you have a generator tool to create the corresponding Java classes
     for you, you could include the details in the "form-bean" declarations.
     Otherwise, you would only define the "form-bean" element itself, with the
     corresponding "name" and "type" attributes, as shown here.
-->


<struts-config>

<!-- ============================================ Data Source Configuration -->
<!-- ===deprecated, use hibernate instead -->

<!-- ================================================ Form Bean Definitions -->

  <form-beans>
    <!-- Registration form bean -->
    <!--form-bean name="registrationForm" type="org.apache.struts.webapp.validator.RegistrationForm" / -->
    <!-- Multi-Part Registration form bean -->
    <!--form-bean name="multiRegistrationForm" type="org.apache.struts.webapp.validator.RegistrationForm" / -->
    <!-- Type form bean -->
    <!--form-bean name="typeForm" type="org.apache.struts.webapp.validator.TypeForm" / -->
    <!-- Locale form bean -->
    <form-bean name="localeForm" type="org.apache.struts.action.DynaActionForm">
      <form-property name="language" type="java.lang.String" />
      <form-property name="country" type="java.lang.String" />
    </form-bean>
    <form-bean name="uploadForm" type="bg.struts.forms.UploadForm" />
    <form-bean name="fileForm" type="bg.struts.forms.FileForm" />   
    <form-bean name="formatForm" type="bg.struts.forms.FormatForm" /> 
  </form-beans>


<!-- ========================================= Global Exception Definitions -->
  <global-exceptions>
    <!-- sample exception handler
    <exception
        key="expired.password"
        type="app.ExpiredPasswordException"
        path="/changePassword.jsp"/>
    end sample -->
  </global-exceptions>

  <global-forwards>
    <forward name="welcome" 
             path="/Welcome.do"/>
    <forward name="home" 
             path="/index.jsp" />
    <forward name="file" 
             path="/pages/workbench/file.jsp"/>              
    <forward name="upload" 
             path="/pages/workbench/upload.jsp"/>           
  </global-forwards>

  <action-mappings>
    <action
        path="/Welcome"
        type="org.apache.struts.webapp.example.WelcomeAction">
        <forward name="failure" 
                 path="/pages/other/Error.jsp"/>
        <forward name="success" 
                 path="/pages/Welcome.jsp"/>
    </action>
    <action path="/file-submit" 
            type="bg.struts.actions.FileAction" 
            name="fileForm" 
            scope="request" 
            validate="false" 
            input="input">    
       <forward name="input" 
	        path="/pages/workbench/file.jsp"/>
       <forward name="display" 
	        path="/pages/workbench/format.jsp"/>
       <forward name="error" 
	        path="/pages/workbench/error.jsp"/>
    </action>
    <action path="/format-submit" 
            type="bg.struts.actions.FormatAction" 
            name="formatForm" 
            scope="request" 
            validate="false" 
            input="input">    
       <forward name="input" 
	        path="/pages/workbench/format.jsp"/>
       <forward name="display" 
	        path="/pages/workbench/georef.jsp"/>
       <forward name="error" 
	        path="/pages/workbench/error.jsp"/>
    </action>   
    <action path="/georef-submit" 
            type="bg.struts.actions.GeorefAction" 
            name="georefForm" 
            scope="request" 
            validate="false" 
            input="input">    
       <forward name="input" 
	        path="/pages/workbench/format.jsp"/>
       <forward name="display" 
	        path="/pages/workbench/table.jsp"/>
       <forward name="error" 
	        path="/pages/workbench/error.jsp"/>
    </action>      
    <action path="/upload-submit" 
            type="bg.struts.actions.UploadAction" 
            name="uploadForm" 
            scope="request" 
            validate="false" 
            input="input">
            
      <forward name="input" 
               path="/pages/workbench/upload.jsp"/>
      <forward name="display" 
               path="/pages/workbench/format.jsp"/>
      <forward name="error" 
               path="/pages/workbench/error.jsp"/>
    </action>

      <!--should duplicate this for each mgr e.g. don't go to georefmanager on input-->
      <action path="/type-submit" 
              type="org.apache.struts.webapp.validator.TypeAction" 
              name="typeForm" 
              scope="request" 
              validate="true" 
              input="input">
        <forward name="input" 
                 path="/pages/georef/GeorefManager.jsp" />
        <forward name="success" 
                 path="/Welcome.do" />
      </action>

      <action path="/registration" forward="/pages/other/registration.jsp" />

      <action path="/registration-submit" 
              type="org.apache.struts.webapp.validator.RegistrationAction" 
              name="registrationForm" 
              scope="request" 
              validate="true" 
              input="input">
        <forward name="input" 
                 path="/pages/other/registration.jsp" />
        <forward name="success" 
                 path="/pages/configure/ConfigurationManager.jsp" />
      </action>

      <action path="/jsRegistration" 
              forward="/pages/other/jsRegistration.jsp" />

      <!-- Multi-Part Registration Action -->
      <action path="/multiRegistration" 
              forward="/pages/other/multiRegistration1.jsp" />

      <action path="/multiRegistration-submit" 
              type="org.apache.struts.webapp.validator.MultiRegistrationAction" 
              name="multiRegistrationForm" 
              scope="request" 
              validate="false">
        <forward name="success" 
                 path="/pages/configure/ConfigurationManager.jsp" />
        <forward name="input1" 
                 path="/pages/other/multiRegistration1.jsp" />
        <forward name="input2" 
                 path="/pages/other/multiRegistration2.jsp" />
      </action>

      <action path="/editJsType" 
              type="org.apache.struts.webapp.validator.EditTypeAction" 
              scope="request" 
              validate="false">
        <forward name="success" 
                 path="/pages/other/jsType.jsp" />
      </action>

      <action path="/jsType" 
              forward="/pages/other/jsType.jsp" />

      <action path="/jsType-submit" 
              type="org.apache.struts.webapp.validator.TypeAction" 
              name="jsTypeForm" 
              scope="request" 
              validate="true" 
              input="/pages/jsType.jsp">
        <forward name="success" 
                 path="/Welcome.do" />
      </action>

      <!-- Locale Action -->
      <action path="/locale" 
              type="org.apache.struts.webapp.validator.LocaleAction" 
              name="localeForm" 
              scope="request">
        <forward name="success" 
                 path="/pages/configure/ConfigurationManager.jsp" />
      </action>

  </action-mappings>

<!-- ============================================= Controller Configuration -->

  <controller>
     <set-property property="maxFileSize" value="100M"/>
     <set-property property="processorClass" value="org.apache.struts.tiles.TilesRequestProcessor"/>
     <set-property property="inputForward" value="true"/>
  </controller>
  
  <message-resources parameter="edu.biogeomancer.MessageResources" />
  
  <!-- ============================================ Plug Ins Configuration  -->
  <!-- 
       Add multiple validator resource files by setting the pathnames property 
       with a comma delimitted list of resource files to load. 
    -->
  <plug-in className="org.apache.struts.validator.ValidatorPlugIn">
    <set-property property="pathnames" value="/WEB-INF/validator-rules.xml,/WEB-INF/validator/validation.xml" />
    <set-property property="stopOnFirstError" value="true" />
  </plug-in>

  <!-- ======================================================= Tiles plugin -->
  <!--
     This plugin initialize Tiles definition factory. This later can takes some
	 parameters explained here after. The plugin first read parameters from
	 web.xml, thenoverload them with parameters defined here. All parameters
	 are optional.
     The plugin should be declared in each struts-config file.
       - definitions-config: (optional)
            Specify configuration file names. There can be several comma
		    separated file names (default: ?? )
       - moduleAware: (optional - struts1.1)
            Specify if the Tiles definition factory is module aware. If true
            (default), there will be one factory for each Struts module.
			If false, there will be one common factory for all module. In this
            later case, it is still needed to declare one plugin per module.
            The factory will be initialized with parameters found in the first
            initialized plugin (generally the one associated with the default
            module).
			  true : One factory per module. (default)
			  false : one single shared factory for all modules
	   - definitions-parser-validate: (optional)
	        Specify if xml parser should validate the Tiles configuration file.
			  true : validate. DTD should be specified in file header (default)
			  false : no validation

	  Paths found in Tiles definitions are relative to the main context.
  -->

  <plug-in className="org.apache.struts.tiles.TilesPlugin" >
    <set-property property="definitions-config" value="/WEB-INF/tiles-defs.xml" />
    <set-property property="moduleAware" value="false" />
    <set-property property="definitions-parser-validate" value="false" />
  </plug-in>

 <plug-in className="org.apache.struts.webapp.example.memory.MemoryDatabasePlugIn">
    <set-property property="pathname" value="/WEB-INF/database.xml"/>
  </plug-in>

</struts-config>

